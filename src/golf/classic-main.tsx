import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import { ClassicGolfVariant } from './ClassicGolfVariant';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ClassicGolfVariant />
  </React.StrictMode>,
);
